<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright(c) 2021 TechSureCo.,Ltd.AllRightsReserved.
  ~ 本内容仅限于深圳市赞悦科技有限公司内部传阅，禁止外泄以及用于其他的商业项目。
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="codedriver.framework.process.dao.mapper.ProcessTagMapper">
    <select id="getProcessTagForSelect" parameterType="codedriver.framework.process.dto.ProcessTagVo" resultType="codedriver.framework.common.dto.ValueTextVo">
        SELECT
        `id` as value,
        `name` as text
        FROM  `process_tag`
        <if test="keyword != null and keyword != ''">
            WHERE `name` LIKE CONCAT('%', #{keyword}, '%')
            ORDER BY LENGTH(`name`)
        </if>
        <if test="needPage">
            limit #{startNum}, #{pageSize}
        </if>
    </select>

    <select id="getProcessTagByNameList" parameterType="java.lang.String" resultType="codedriver.framework.process.dto.ProcessTagVo">
        SELECT
        `name`,
        `id`
        FROM `process_tag`
        WHERE `name` IN
        <foreach collection="list" item="name" open="(" separator="," close=")">
            #{name}
        </foreach>
    </select>

    <select id="getProcessTagIdByName" parameterType="java.lang.String" resultType="java.lang.Long">
        SELECT `id` FROM `process_tag` WHERE `name` = #{value}
    </select>

    <select id="getProcessTagCount" parameterType="codedriver.framework.process.dto.ProcessTagVo" resultType="java.lang.Integer">
        SELECT
        count(1)
        FROM  `process_tag`
        <if test="keyword != null and keyword != ''">
            WHERE `name` LIKE CONCAT('%', #{keyword}, '%')
        </if>
    </select>

    <insert id="insertProcessTag" parameterType="codedriver.framework.process.dto.ProcessTagVo">
        INSERT INTO `process_tag` (`id`, `name`)
        VALUES (#{id}, #{name})
    </insert>
</mapper>